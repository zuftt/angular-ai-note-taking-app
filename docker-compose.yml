version: '3.8'

services:
  mongo:
    image: mongo:7
    container_name: notes-app-mongo
    ports:
      - "27017:27017"
    volumes:
      - ./data:/data/db
    environment:
      MONGO_INITDB_DATABASE: notes-app
